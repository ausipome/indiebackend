"use strict";
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var import_ensureDatabaseExists = require("./ensureDatabaseExists");
async function main() {
  await (0, import_ensureDatabaseExists.askToCreateDb)("file:dev7.db", "apply", ".");
}
__name(main, "main");
void main();
